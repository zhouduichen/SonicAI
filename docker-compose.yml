version: "3.9"

services:
  redis:
    image: redis:7-alpine
    container_name: aimusic-redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis_data:/data

volumes:
  redis_data:
